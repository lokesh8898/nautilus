# nautilus
This repository contains code for a two-stage data transformation pipeline: first converting GFDL data into Parquet format, and then transforming the Parquet data into the Nautilus format.
